pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url 'https://repo.papermc.io/repository/maven-public/' }
    }
}

rootProject.name = 'HuskTowns'
include(
        'common',
        'bukkit',
        'paper'
)